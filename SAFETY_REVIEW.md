# 请求额度与执行安全修复说明

## 状态与边界

基线为上游 `f861c696e32af3990c2322db16d698a982bce874`。本分支只修改 Python 执行层及测试；不修改报价算法 `quoting.py`、Agent 签名 `orders.py`、手续费公式、下单金额/杠杆配置或遗留油猴脚本。没有部署、合并或进行真实交易。

这是一版保守的修复候选，不是安全审计认证，不保证降低交易损耗或一定平仓。停止新增风险，有时意味着成交量下降。请求额度、网络、成交和保证金风险仍由平台与行情决定。

## 修改前后及原理

| 场景 | 修改前 | 本分支 |
| --- | --- | --- |
| 启动已欠额度 | 仍更新杠杆并尝试开仓 | 先只读检查；缺额度/未知状态不发送签名操作 |
| 30 秒退避结束 | 恢复尝试新单 | 禁止开仓锁不会因时间解除；需人工核对再重启 |
| 累计限额时有仓 | 共用退避阻止退出 | 撤单、reduce-only 退出分别节流；不绕过交易所限额 |
| 网络超时 | 误当成普通 429 | 视为 Unknown outcome；保留 CLOID，禁发新订单，人工核对 |
| 批量单一边拒绝 | 可能漏记另一边成功 | 检查每项结果；部分拒绝不覆盖已成功侧 |
| IOC 成交 | filled 可能被存成挂单 | 不创建挂单缓存；重新查询剩余仓位 |
| 撤单 | 同一单 OID 和 CLOID 都撤 | 有 OID 则只用 OID；结果不明确不直接替换 |
| 仓位查询失败 | 缓存继续参与决策 | 保留缓存但失去可信标记，禁止用未知仓位发新订单 |
| 旧盘口 + 查询失败 | 可能继续用旧数据报价 | 停止报价并尝试撤单；耗时过久的计划丢弃 |
| Dead-man 失败 | 本地退避时间可能当成有效期限 | 重试时间和已确认期限分开记录 |
| Ctrl+C | 仅撤单，可能成功退出却有仓位 | 仍默认仅撤单，但核对仓位；不干净则非零退出 |

为什么保留原 Python 架构：问题在订单执行状态和请求调度，不在语言速度。把额度解析独立为纯逻辑 `safety.py`，可以用无网络测试验证；价格、签名与手续费模块保持不动，减少同时改变多个因素。

为什么不自动重发超时订单：超时只说明客户端没有收到响应，不证明服务器没有执行。若另造 CLOID 重发，可能得到两笔订单。本版选择停止订单发送、保留原 CLOID 用于取消，并要求人工核对。**本版尚未实现自动 orderStatus 对账恢复、持久化订单日志或跨主机锁。不要通过盲目重启解除未知状态。**

## 额度与时序

- `userRateLimit` 每 15 秒查询，快照超过 20 秒不允许开仓。
- 观察值为 `cap - used + surplus`。每个已发动作先在本地预扣；旧 API 快照不会归还预扣。失败动作也保守计入，可能提早停止，但不会伪造额度。
- 每个配置标的预留 25 个动作，再加本次双边单 2 个动作。25 是工程缓冲，不是官方规定，也不足以保证任何情况下退出。预留低于阈值会锁定禁止开仓。
- 普通退出重试间隔至少 3 秒；触发累计限额后的退出间隔至少 11 秒。撤单额外额度并非无限；撤单遭限额后也退避。批量动作按订单数预扣。
- `clearinghouseState` 仍保留低延迟轮询；WS 成交通知只标记仓位需要重查，不直接累加仓位，避免快照/补漏重复计算。
- REST `userFills` 补漏从 2 秒变为 30 秒，WS 的 +3 秒 markout 不变。WS 中断期间补漏 markout 可能缺失，不应当作可比的高质量样本。
- info 429 后暂缓查询 5 秒。此次并未实现完整共享 IP token bucket；同 IP 上其他程序仍会影响额度。不能认为这些节流参数适合任意多实例。

## 运行前必须理解

1. 使用专用账户，不得同时运行旧版、新版或其他机器人。相同标的的手工仓位与机器人仓位在交易所是合并的，程序无法区分其所有权。
2. 启动要求 `io` 状态可读、配置标的空仓且 `io` 没有挂单。**不会自动接管旧遗留仓位。** 其他 DEX/现货账户仍需要你自己检查；Dead-man 会影响整个主账户。
3. EBOT 标记用于识别机器人订单，不是不可伪造的安全身份。不要与使用相同标记的其他实例共用账户。
4. Ctrl+C、`--seconds` 到时、断网、kill -9 都不等于平仓。退出仍需网页核对仓位和挂单；默认没有自动停止平仓功能。
5. 如 `SAFETY entry_halt`：停止增加风险，先核对账户。若 `unknown_write` 或 `SHUTDOWN NOT CLEAN`：人工处理，不能反复重启。
6. 不要为了补额度直接扩大金额、买额度或改用 Taker。代码修复不能清除历史欠额，也不会替你购买请求动作。
7. 保留外部凭据文件，不复制进仓库；示例值保持为空。测试不需要任何真实私钥。
8. 本轮不启用 systemd 自动重启。今后如使用服务，需明确处理安全拒绝退出码，不能形成“失败—重启—再下单”的循环。

## 验证记录

在独立本地 Python 3.12 环境，原版 68 项测试通过，修复后 104 项测试通过。测试默认禁用网络连接；只使用模拟 Client/Signer 与测试固定值，没有连接真实账户或运行 live/paper 网络会话。

覆盖：初始欠额度、缺失账户状态、未平仓启动、旧额度快照、撤单期间成交、批量一边成功一边拒绝、缺失订单状态、撤单返回错误、IOC 成交、超时、旧盘口、Dead-man 拒绝、遗留仓位退出、限额中 reduce-only 通道。

主要已测依赖：hyperliquid-python-sdk 0.24.0、eth-account 0.13.7、eth-utils 5.3.1、requests 2.34.2、websocket-client 1.9.2、pytest 9.1.1。保留上游依赖声明，**没有验证与服务器原虚拟环境完全一致**；Ubuntu 上需用既有版本另外跑一轮测试，不要顺手升级生产依赖。

本地验证命令（没有交易功能）：

```sh
.venv/bin/python -m pytest
git diff --check
```

测试不能证明：真实撮合队列、交易所查询延迟、长期损耗、极端行情、断网下的平仓可达性、平台真实限额的所有细节。下一步应先复核 PR，再做 Ubuntu 离线测试；实盘和合并需另行授权。

## 可核验依据

- [Hyperliquid 官方额度规则](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/rate-limits-and-user-limits)：区分 IP 和账户累计限制、受限动作与撤单额度，以及批量计数。
- [Hyperliquid 官方 info 接口](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint#query-user-rate-limits)：账户额度字段与订单/仓位查询。
- [Hyperliquid 官方 exchange 接口](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)：逐项 order/cancel 结果、ALO/IOC 与账户级 Dead-man。
- [NautilusTrader Hyperliquid 执行适配器文档](https://nautilustrader.io/docs/nightly/integrations/hyperliquid/#post-deadlines-and-retries)：请求发送后结果未知时避免盲目重发。这里借鉴其执行原则，没有引入其框架或签名脚本；nightly 文档可能变化。

## 学习核对

请能解释这几个问题后再申请实盘：为什么等 30 秒不能补累计额度？为什么 HTTP 200 不等于批量全成功？为什么 canceled 与 flat 不一样？为什么 timeout 后不能换 CLOID 直接重发？为什么相同标的不能让手动交易与机器人共用仓位？为什么测试通过不代表已经验证交易成本？
